package com.example.randomizerapp.ui.theme

import androidx.compose.ui.graphics.Color

val DarkBackground = Color(0xFF1A1A1A) // Очень темный фон
val DarkSurface = Color(0xFF2C2C2C)    // Для карточек, кнопок
val PrimaryGreen = Color(0xFF00FF00) // Ярко-зеленый (для акцентов, иконок)
val TextLight = Color(0xFFE0E0E0)      // Светлый текст
val TextDark = Color(0xFF333333)       // Темный текст на светлом фоне (если нужен)
val ButtonBackground = Color(0xFF00C853) // Цвет кнопок
val ButtonText = Color(0xFFFFFFFF)